package com.example.light;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class LightApplicationTests {

	@Test
	void contextLoads() {
//		String binaryStr  = "0101010101";
//		String paddedStr = String.format("%16s", binaryStr).replace(' ', '0');
//
//		// 将补齐后的字符串转化为十六进制字符串
//		long intValue = Long.parseLong(paddedStr, 2);
//		String hexStr = String.format("%04X", intValue);
//
//		System.out.println(hexStr);
//
//		// 将十六进制字符串转化为整数
//		intValue = Integer.parseInt(hexStr, 16);
//
//		// 将整数转化为二进制字符串
//		binaryStr = String.format("%16s", Long.toBinaryString(intValue)).replace(' ', '0');
//		System.out.println(binaryStr);

	}

	public static void main(String[] args) {
//		String binaryStr  = "01";
//		String paddedStr = String.format("%4s", binaryStr).replace(' ', '0');
//
//		// 将补齐后的字符串转化为十六进制字符串
//		long intValue = Long.parseLong(paddedStr, 2);
//		String hexStr = String.format("%04X", intValue);
//
//		System.out.println("设备序列号："+hexStr+"0001");
//
//		intValue = Integer.parseInt(hexStr, 16);
//
//		// 将整数转化为二进制字符串
//		binaryStr = String.format("%4s", Long.toBinaryString(intValue)).replace(' ', '0');
//		System.out.println(binaryStr);

//		int decimalNumber = 11;
//		String hexString = String.format("%04X", decimalNumber);
//		System.out.println(hexString);
	}

}
